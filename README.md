# Спринт 2
В корне содержится compose.yaml для запуска начального приложения. Для запуска выполнить:

```shell
docker compose up -d
```
После этого необходимо заполнить данными mongodb:


```shell
./scripts/mongo-init.sh
```
## Задание 1
В корне лежат все схемы спринта:
- task1.1 - схема с шардированием - mongo-sharding
- task1.2 - схема с шардирование и репликацией - mongo-sharding-repl
- task1.3 - схема с шардированием, репликацией и кэшированием - sharding-repl-cache

## Задание 2
В папке mongo-sharding находится реализация с шардированием (схема task1.1). Для запуска зайти в папку и выполнить:

```shell
docker compose up -d
```
После этого необходимо выполнить инициализацию:

```shell
./scripts/init.sh
```

## Задание 3
В папке mongo-sharding-repl находится реализация с шардированием и репликацией (схема task1.2). Для запуска зайти в папку и выполнить:

```shell
docker compose up -d
```
После этого необходимо выполнить инициализацию:

```shell
./scripts/init.sh
```

## Задание 4
В папке sharding-repl-cache находится реализация с шардированием, репликацией и кэшированием (схема task1.3). Для запуска зайти в папку и выполнить:

```shell
docker compose up -d
```
После этого необходимо выполнить инициализацию:

```shell
./scripts/init.sh
```

## Задание 5
Схема с балансировкой - task5.drawio

## Задание 6
Схема с CDN - task6.drawio

## Как проверить

### Если вы запускаете проект на локальной машине

Откройте в браузере http://localhost:8080

### Если вы запускаете проект на предоставленной виртуальной машине

Узнать белый ip виртуальной машины

```shell
curl --silent http://ifconfig.me
```

Откройте в браузере http://<ip виртуальной машины>:8080

## Доступные эндпоинты

Список доступных эндпоинтов, swagger http://<ip виртуальной машины>:8080/docs
